import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

@NgModule({
  declarations: [
    // Aquí registraremos tus componentes uno por uno
  ],
  imports: [
    CommonModule
  ],
  exports: [
    // Aquí exportaremos los mismos componentes
  ]
})
export class SharedComponentsModule {}
